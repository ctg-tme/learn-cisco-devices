# URL Rewriting for media proxy routes
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /learn-cisco-devices/
    
    # Extension-less hierarchical media proxy (most specific, check first)
    # /{deployment-type}/{device-type}/{name} -> serve index.html (SPA handles redirect and auto-detects extension)
    # Example: /mtr/navigator/schedule_teams
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^[^/]+/[^/]+/[^/\.]+$ index.html [L]
    
    # New hierarchical media proxy WITH extension
    # /{deployment-type}/{device-type}/{filename.ext} -> serve index.html (SPA handles redirect)
    # Example: /mtr/navigator/filename.webm
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^[^/]+/[^/]+/[^/]+\.(webm|mp4|png|jpg|jpeg|gif)$ index.html [L]
    
    # Hierarchical media proxy routes WITH type prefix
    # /videos/{deployment-type}/{device-type}/{filename} -> serve index.html (SPA handles redirect)
    # /images/{deployment-type}/{device-type}/{filename} -> serve index.html (SPA handles redirect)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(videos|images)/[^/]+/[^/]+/.+$ index.html [L]
    
    # Simplified media proxy routes
    # /videos/{deployment}/{filename} -> serve index.html (SPA handles redirect)
    # /images/{deployment}/{filename} -> serve index.html (SPA handles redirect)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(videos|images)/[^/]+/.+$ index.html [L]
    
    # Legacy media proxy routes (for backward compatibility)
    # /media/{deployment}/{type}/{filename} -> serve index.html (SPA handles redirect)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^media/[^/]+/(videos|images)/.+$ index.html [L]
    
    # General SPA routing - serve index.html for all other non-file routes
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.html [L]
</IfModule>

# Security Headers for Apache/GitHub Pages fallback
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "DENY"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline'; img-src 'self' data:; media-src 'self'; connect-src 'self' https://api-us.aptabase.com; object-src 'none'; frame-src 'none'; frame-ancestors 'none'; base-uri 'self'; form-action 'none';"
    Header always set Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()"
</IfModule>

# Prevent access to sensitive files
<Files "manifest.local.json">
    Order allow,deny
    Deny from all
</Files>

<Files ".env*">
    Order allow,deny
    Deny from all
</Files>
